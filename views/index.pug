html
  head
    title message board
  body
    h1 message board
    a(href="new") add message

    ul
      each message in messages
        li
          strong= message.user || "Unknown User"
          | : #{message.text || "No Text"}
